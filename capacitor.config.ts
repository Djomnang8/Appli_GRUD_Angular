import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.gestion_employes',
  appName: 'gestion_employes',
  webDir: 'dist/tp1/browser'
};

export default config;
